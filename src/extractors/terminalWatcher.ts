// TODO: Implement terminal output capture for future versions
import * as vscode from 'vscode';

/** Placeholder for terminal output watcher. */
export class TerminalWatcher implements vscode.Disposable {
  dispose(): void {
    // Cleanup
  }
}
